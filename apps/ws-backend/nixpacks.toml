[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.0.0", "cd ../.. && pnpm install"]

[phases.build]
cmds = ["cd ../.. && pnpm --filter ws-backend build"]

[start]
cmd = "cd apps/ws-backend && node ./dist/index.js"
